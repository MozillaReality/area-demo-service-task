README



The app requires SharedArrayBuffer support. In Firefox, this can be enabled by typing `about:config` in the URL bar, and in the list of configuration options, toggling the option `javascript.options.shared_memory` to ON.